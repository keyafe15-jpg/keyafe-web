# Shared upstream — load before site configs
# sudo cp deploy/nginx/00-keyafe-upstream.conf /etc/nginx/conf.d/keyafe-upstream.conf

upstream keyafe_api {
    server 127.0.0.1:4000;
    keepalive 8;
}
